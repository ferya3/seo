# سرویس internal-links — گراف لینک داخلی
fastapi>=0.115
uvicorn[standard]>=0.32
pika>=1.3
jsonschema>=4.21
# فقط برای خواندن گزارش خزش از سرویس Crawl
requests>=2.32

# ذخیره‌سازی روی Postgres. اختیاری: بدون DATABASE_URL سرویس روی فایل کار می‌کند.
psycopg[binary,pool]>=3.2
