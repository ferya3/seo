# معماری — وضعیت فعلی

این سند می‌گوید از معماری هدف **چه چیزی ساخته و تست شده**، چه چیزی داربست است،
و چه چیزی هنوز شروع نشده. هدف این است که با نگاه به پوشه‌ها فکر نکنی چیزی آماده
است در حالی که نیست.

## وضعیت

| بخش | وضعیت | توضیح |
| --- | --- | --- |
| موتور سئو (`services/engine`) | ✅ کار می‌کند، ۱۵۶ تست | خزش، ۴۰+ قانون، امتیازدهی، تحقیق کلمات کلیدی، داشبورد |
| قراردادها (`shared/contracts`) | ✅ کار می‌کند | envelope + ۴ رویداد، با اعتبارسنجی JSON Schema |
| باس رویداد (`shared/events`) | ✅ کد کامل | publisher/consumer روی RabbitMQ با DLQ و publisher confirms — منطقش تست شده، اتصال واقعی به بروکر در این محیط تست نشده |
| سرویس Crawl (`services/crawl`) | ✅ کار می‌کند، ۲۱ تست | API با FastAPI + ورکر باس، هر دو روی یک `run_crawl` |
| اسکیمای دیتابیس (`infra/db`) | ⚠️ نوشته شده، اجرا نشده | نحو با پارسر Postgres تأیید شد؛ روی سرور واقعی اجرا نشده (اینجا Postgres نبود) |
| docker-compose | ⚠️ نوشته شده، اجرا نشده | Postgres، Redis، RabbitMQ، Qdrant، Meilisearch، MinIO + سرویس‌های ما (اینجا داکر دیمن نبود) |
| API Gateway (`apps/api-gateway`) | ⚠️ داربست + کلاینت | Laravel 13 نصب شد ولی `vendor/` در این محیط ساخته نشد (composer به github دسترسی نداشت). کد PHP فقط `php -l` شده — **اجرا و تست نشده** |
| بقیه سرویس‌ها و ایجنت‌ها | ❌ شروع نشده | SERP، Content، Optimizer، Internal Links، Competitor، Rank، GSC، Orchestrator |
| Frontend (Nuxt) | ❌ شروع نشده | داشبورد فعلی همان Flask داخل موتور است |
| k8s / terraform / monitoring | ❌ شروع نشده | |

## چرا این ترتیب

قرارداد و باس اول ساخته شد چون **سطح یکپارچگی** هستند: تا وقتی شکل رویدادها
مشخص نباشد، هر سرویس جدیدی که نوشته شود بعداً باید بازنویسی شود. سرویس Crawl
دوم بود چون تنها بخشی است که منطق واقعی و آماده دارد — بقیه‌ی سرویس‌ها هنوز
منطقی ندارند که سرویس‌شان کنیم.

## تصمیم‌های مهم

**موتور دور ریخته نشد.** `services/engine` همان کد کارکرده است که حالا پشت
سرویس Crawl قرار گرفته. سرویس Crawl یک لایه‌ی نازک روی آن است، نه بازنویسی.

**یک مسیر اجرا، دو ورودی.** هم `POST /v1/crawls` و هم رویداد `crawl.requested`
به یک تابع `run_crawl` می‌رسند. اگر دو مسیر جدا بودند، رفتارشان با هم فرق
می‌کرد و باگ‌ها فقط در یکی دیده می‌شد.

**رویدادها خلاصه‌اند، نه کامل.** `crawl.completed` فقط امتیاز و آمار را حمل
می‌کند و آدرس گزارش کامل را می‌دهد. یک گزارش کامل چند مگابایت است و داخل پیام
جا نمی‌شود.

**خطای بروکر، خزش موفق را خراب نمی‌کند.** نتیجه قبلاً روی دیسک ذخیره شده؛ اگر
انتشار رویداد شکست بخورد لاگ می‌شود ولی وضعیت به «شکست‌خورده» تغییر نمی‌کند.

**پیام نامعتبر هرگز منتشر نمی‌شود.** قبل از انتشار، payload با قرارداد
اعتبارسنجی می‌شود. نقض قرارداد باگ ماست و نباید به همه‌ی مصرف‌کننده‌ها پخش شود.

**تحویل at-least-once است.** ورکر روی `envelope.id` و روی وضعیت خود crawl
idempotent است. پیام خراب به DLQ می‌رود نه اینکه بی‌نهایت بار دوباره تلاش شود.

**جدول outbox.** در اسکیما هست چون نوشتن وضعیت و انتشار رویداد باید در یک
تراکنش باشند؛ وگرنه کرش بین این دو، رویداد را بی‌صدا گم می‌کند. هنوز استفاده
نشده — وقتی سرویس‌ها به Postgres وصل شوند فعال می‌شود.

## قدم بعدی

۱. `composer install` روی ماشینی با دسترسی GitHub، و تست واقعی Gateway
۲. بالا آوردن docker-compose و اجرای مهاجرت‌ها روی Postgres واقعی
۳. انتقال `CrawlStore` از فایل به Postgres + فعال کردن outbox
۴. سرویس بعدی: SERP یا Keyword (هر دو منطقشان در موتور آماده است)
۵. Orchestrator و ایجنت‌ها
