# معماری — وضعیت فعلی

این سند می‌گوید از معماری هدف **چه چیزی ساخته و تست شده**، چه چیزی داربست است،
و چه چیزی هنوز شروع نشده. هدف این است که با نگاه به پوشه‌ها فکر نکنی چیزی آماده
است در حالی که نیست.

## وضعیت

| بخش | وضعیت | توضیح |
| --- | --- | --- |
| موتور سئو (`services/engine`) | ✅ کار می‌کند، ۱۵۶ تست | خزش، ۴۰+ قانون، امتیازدهی، تحقیق کلمات کلیدی، داشبورد |
| قراردادها (`shared/contracts`) | ✅ کار می‌کند | envelope + ۱۹ رویداد، با اعتبارسنجی JSON Schema |
| باس رویداد (`shared/events`) | ✅ کار می‌کند، روی RabbitMQ 3.12 واقعی تست شد | publisher/consumer با publisher confirms؛ مسیر dead-letter هم اجرا شد: هندلری که خطا داد دقیقاً یک‌بار صدا زده شد و پیام در `.dlq` نشست، نه در حلقه‌ی بی‌نهایت |
| ذخیره‌سازی فایلی (`shared/store.py`) | ✅ کار می‌کند | `FileJobStore` عمومی — حالت بدون دیتابیس، همان چیزی که نصب تک‌ماشینه استفاده می‌کند |
| ذخیره‌سازی Postgres + outbox (`shared/db.py`) | ✅ کار می‌کند، ۲۷ تست روی Postgres 16 واقعی | همان اینترفیس `FileJobStore`، به‌علاوه نوشتن وضعیت و رویداد در **یک تراکنش** |
| رله outbox (`shared/relay.py`) | ✅ کار می‌کند، مسیر کامل روی Postgres + RabbitMQ واقعی اجرا شد | outbox → بروکر → مصرف‌کننده؛ `event_id` استیج‌شده تا انتهای مسیر همان می‌ماند |
| سرویس Crawl (`services/crawl`) | ✅ کار می‌کند، ۲۱ تست | API با FastAPI + ورکر باس، هر دو روی یک `run_crawl` |
| سرویس Keyword (`services/keyword`) | ✅ کار می‌کند، ۲۳ تست | API با FastAPI + ورکر باس، هر دو روی یک `run_research` |
| سرویس SERP (`services/serp`) | ⚠️ منطق ✅ (۴۱ تست)، **fetch تأییدنشده** | رتبه‌یابی، رقبا، اولویت‌بندی — همه تست‌شده؛ ولی provider اینجا اجرا نشد چون همه‌ی موتورهای جستجو بلاک‌اند |
| اسکیمای دیتابیس (`infra/db`) | ✅ روی Postgres 16 واقعی اجرا شد | هر سیزده مهاجرت از صفر روی دیتابیس خالی، با `migrate.sh` — و اجرای دوم صفر مهاجرت اعمال کرد |
| docker-compose | ⚠️ اجرا نشده، ولی **۲۱ بررسی خودکار** رویش هست | ۳۰ سرویس؛ هر چیزی که بدون داکر دیمن قابل تصمیم‌گیری است (مسیر ماژول، پورت، volume، وابستگی، متغیر لازم) روی هر اجرای تست بررسی می‌شود |
| API Gateway (`apps/api-gateway`) | ✅ کار می‌کند، ۱۵۰ تست روی Postgres واقعی | Laravel 13 روی PHP 8.4؛ Sanctum، تنانسی از توکن، rate limit، ترجمه‌ی خطای سرویس‌ها |
| Auth و Projects | ✅ کار می‌کند | ثبت‌نام/ورود/خروج + CRUD پروژه، همان جدول‌های `tenants`/`users`/`projects` که سرویس‌ها به آن‌ها FK دارند |
| سرویس Internal Links (`services/internal_links`) | ✅ کار می‌کند، ۴۰ تست | گراف لینک داخلی: اعتبار داخلی، صفحات یتیم و نادیده‌گرفته‌شده، لینک به صفحه‌ی خراب، انکرتکست |
| سرویس Content (`services/content`) | ✅ کار می‌کند، ۵۸ تست | پوشش کلمات کلیدی، شکاف محتوا، رقابت داخلی صفحات — تنها تحلیلی که همزمان به دو مرحله نیاز دارد |
| سرویس Competitor (`services/competitor`) | ✅ کار می‌کند، ۶۰ تست + مقایسه‌ی واقعی داخل گردش‌کار | سایت شما در برابر رقبا: نظم عنوان و توضیح، طول محتوا، و موضوع‌هایی که پوشش نمی‌دهید |
| سرویس Optimizer (`services/optimizer`) | ✅ کار می‌کند، ۴۹ تست | اولین سرویسی که پیشنهاد می‌دهد نه گزارش: چه صفحه‌ای، چه چیزش، و متن جایگزین |
| سرویس Reporting (`services/reporting`) | ✅ کار می‌کند، ۵۵ تست + PDF واقعی گرفته شد | گردش‌کار → سند خودبسنده‌ی HTML و Markdown؛ خودکار وقتی گردش‌کار تمام می‌شود |
| سرویس Notifications (`services/notifications`) | ✅ کار می‌کند، ۶۰ تست + تحویل واقعی روی هر دو ترابری | وبهوک امضاشده و ایمیل؛ یک‌بار فرستادن با ایندکس یکتا، آدرس وبهوک از SSRF guard رد می‌شود |
| Orchestrator (`agents/orchestrator`) | ✅ کار می‌کند، ۱۷۳ تست روی Postgres واقعی | گردش‌کار شش‌مرحله‌ای (+ یک مرحله به ازای هر رقیب) + خلاصه‌ی اجرایی + زمان‌بندی + روند نسبت به اجرای قبلی؛ حلقه‌ی کامل روی RabbitMQ و Postgres واقعی اجرا شد |
| لایه‌ی مدل زبانی (`shared/llm`) | ⚠️ منطق ✅ (۴۷ تست با لایه‌ی بازنویسی)، **تماس زنده تأییدنشده** | تنها دروازه‌ی پروژه به Claude؛ اینجا `ANTHROPIC_API_KEY` نبود، پس هیچ درخواستی واقعاً ارسال نشد — همه‌ی رفتارها با کلاینت بدلی تست شدند |
| بقیه سرویس‌ها و ایجنت‌ها | ❌ شروع نشده | GSC، Backlinks، Analytics — هر سه به API خارجی نیاز دارند که از اینجا در دسترس نیست |
| Frontend (`apps/web-dashboard`) | ✅ کار می‌کند، ۳۵ تست + اجرای واقعی در مرورگر | Nuxt 4 روی گیت‌وی: ورود، پروژه‌ها، شروع تحلیل، گزارش، روند و سابقه، زمان‌بندی، اطلاع‌رسانی. با Chromium روی کل پشته درایو شد |
| k8s / terraform / monitoring | ❌ شروع نشده | |

## چرا این ترتیب

قرارداد و باس اول ساخته شد چون **سطح یکپارچگی** هستند: تا وقتی شکل رویدادها
مشخص نباشد، هر سرویس جدیدی که نوشته شود بعداً باید بازنویسی شود. سرویس Crawl
دوم بود چون تنها بخشی است که منطق واقعی و آماده دارد — بقیه‌ی سرویس‌ها هنوز
منطقی ندارند که سرویس‌شان کنیم.

سرویس Keyword سوم بود، پیش از مهاجرت به Postgres. دلیلش: یک نمونه ثابت نمی‌کند
که الگوی استخراج سرویس درست است. با سرویس دوم معلوم شد چه چیزی واقعاً مشترک
است (ذخیره‌سازی و انتشار رویداد) و چه چیزی مخصوص هر سرویس (شکل رکورد و
قرارداد). مهاجرت ذخیره‌سازی بعد از آن یک‌بار برای هر دو انجام شد، نه دوبار —
`PostgresJobStore` یک کلاس است و هر سرویس فقط یک `JobTable` دارد.

## اجرای تست‌ها

بدون دیتابیس، تست‌های Postgres skip می‌شوند و بقیه کار می‌کنند:

```bash
pytest                       # ۵۷۶ پاس، ۱۵۹ skip
```

با دیتابیس واقعی:

```bash
createdb seo
DATABASE_URL=postgresql://seo@127.0.0.1/seo infra/db/migrate.sh
TEST_DATABASE_URL=postgresql://seo@127.0.0.1/seo pytest    # ۷۵۸ تست
```

`shared/tests` عمداً با فیک اجرا نمی‌شود: ارزش این ذخیره‌ساز اتمی بودن تراکنش
است و تراکنش را نمی‌شود با mock ثابت کرد.

## تصمیم‌های مهم

**ذخیره‌سازی یک‌بار نوشته شد، نه به ازای هر سرویس.** هر سرویس فقط یک زیرکلاس
رکورد دارد که نام‌های دامنه‌ی خودش را بیرون می‌دهد (`crawl_id`/`start_url` در
برابر `research_id`/`seed`). نام‌های عمومی مرز ذخیره‌سازی را رد نمی‌کنند، چون
API و قرارداد رویداد با نام دامنه حرف می‌زنند. وقتی نوبت Postgres رسید، همین
شکل جواب داد: `PostgresJobStore` یک کلاس شد و تفاوت دو سرویس در یک `JobTable`
توصیف شد، نه در یک زیرکلاس به ازای هر سرویس.

**جدول‌های دامنه، نه یک جدول `jobs`.** `crawls` و `research` ستون‌ها، قیدها و
ایندکس‌های خودشان را دارند. یک `CHECK (status IN (...))` که غلط تایپی را سر
نوشتن می‌گیرد، از راحتی یک جدول مشترک ارزشمندتر است. مقادیری مثل
`overall_score` و `total` عمداً هم داخل JSONB هستند و هم ستون واقعی: صفحه‌ی
لیست نباید برای نشان دادن یک امتیاز کل گزارش را باز کند.

**موتور دور ریخته نشد.** `services/engine` همان کد کارکرده است که حالا پشت
سرویس Crawl قرار گرفته. سرویس Crawl یک لایه‌ی نازک روی آن است، نه بازنویسی.

**یک مسیر اجرا، دو ورودی.** هم `POST /v1/crawls` و هم رویداد `crawl.requested`
به یک تابع `run_crawl` می‌رسند. اگر دو مسیر جدا بودند، رفتارشان با هم فرق
می‌کرد و باگ‌ها فقط در یکی دیده می‌شد.

**رویدادها خلاصه‌اند، نه کامل.** `crawl.completed` فقط امتیاز و آمار را حمل
می‌کند و آدرس گزارش کامل را می‌دهد. یک گزارش کامل چند مگابایت است و داخل پیام
جا نمی‌شود.

**خطای بروکر، خزش موفق را خراب نمی‌کند.** در حالت فایلی نتیجه قبلاً ذخیره شده؛
اگر انتشار رویداد شکست بخورد لاگ می‌شود ولی وضعیت به «شکست‌خورده» تغییر
نمی‌کند. در حالت Postgres اصلاً این مسئله پیش نمی‌آید: سرویس روی بروکر
نمی‌نویسد، فقط در outbox می‌نویسد، و بروکر خاموش صرفاً یعنی رله بعداً تحویل
می‌دهد.

**پیام نامعتبر هرگز منتشر نمی‌شود.** قبل از انتشار، payload با قرارداد
اعتبارسنجی می‌شود. نقض قرارداد باگ ماست و نباید به همه‌ی مصرف‌کننده‌ها پخش شود.

**تحویل at-least-once است.** ورکر روی `envelope.id` و روی وضعیت خود crawl
idempotent است. پیام خراب به DLQ می‌رود نه اینکه بی‌نهایت بار دوباره تلاش شود.

**جدول outbox حالا فعال است.** نوشتن وضعیت job و رویدادی که باید منتشر شود در
یک تراکنش انجام می‌شود. تستی که این را ثابت می‌کند عمداً `_stage` را می‌شکند و
بررسی می‌کند که وضعیت هم برنگشته باشد — «crawl تمام‌شده بدون رویداد» دقیقاً
همان چیزی است که outbox جلویش را می‌گیرد.

**اول منتشر کن، بعد علامت بزن.** رله ابتدا روی بروکر منتشر می‌کند و بعد
`published_at` را می‌نویسد. اگر بین این دو کرش کند، رویداد دوباره منتشر می‌شود
— at-least-once، که همه‌ی مصرف‌کننده‌های اینجا با `envelope.id` مدیریتش
می‌کنند. ترتیب برعکس at-most-once می‌داد، و رویداد گم‌شده برخلاف رویداد تکراری
قابل جبران نیست.

**`producer` در خود outbox ذخیره می‌شود.** این را اجرای واقعی نشان داد، نه
تست: در اولین اجرای سرتاسری همه‌ی رویدادها با `producer=outbox-relay` به
مصرف‌کننده رسیدند و معلوم نبود کدام سرویس آن‌ها را ساخته. حالا سرویس سازنده در
جدول می‌نشیند و رله فقط حملش می‌کند.

**گیت‌وی خطای سرویس را ترجمه می‌کند، پنهان نمی‌کند.** سرویس در دسترس نیست →
۵۰۳ (قابل تلاش مجدد)، ورودی بد → ۴۲۲ با **متن خود سرویس**، رکورد ناموجود →
۴۰۴. نسخه‌ی اول این کد از `PendingRequest::throw()` استفاده می‌کرد که شبیه همین
به نظر می‌رسد و نیست: روی هر پاسخ خطا throw می‌کند (پس `if (status === 404)`
زیرش کد مرده بود) و برای `ConnectionException` اصلاً صدا زده نمی‌شود. نتیجه
این بود که **هر خطای پایین‌دستی به ۵۰۰ تبدیل می‌شد**. `php -l` این را نمی‌بیند؛
فقط اجرا کردنش دید.

**تنانت ناشناخته، خطای ۵۰۰ نیست.** `ForeignKeyViolation` به `UnknownTenant`
و بعد به ۴۰۰ سرویس و ۴۲۲ گیت‌وی تبدیل می‌شود، با متن قابل‌فهم.

**یک دیتابیس، نه دو تا.** ریشه‌ی مشکل بالا این بود که گیت‌وی کاربرهایش را در
SQLite خودش نگه می‌داشت و `tenants` جای دیگری بود، و هیچ‌چیز آن دو را همگام
نمی‌کرد. حالا گیت‌وی روی همان Postgres سرویس‌ها می‌نویسد و فایل‌های
`infra/db/migrations/` مرجع اسکیمای آن هستند — به همین دلیل ستون
`password_hash` به `password` لاراولی تغییر نام نداد: ستون مشترک است و
فریم‌ورک فقط یکی از مصرف‌کننده‌هایش. هویت و تنانسی‌ای که سرویس‌ها اعمال
می‌کنند باید **همان ردیف‌ها** باشند، وگرنه کلید خارجی فقط تزئین است.

**`project_id` هم باید بررسی شود، نه فقط `tenant_id`.** تنانت امن بود چون
هیچ‌وقت از بدنه‌ی درخواست نمی‌آمد؛ ولی `project_id` می‌آمد و بدون بررسی جلو
فرستاده می‌شد. کلید خارجی فقط ثابت می‌کند پروژه *وجود دارد*، نه اینکه مال
درخواست‌کننده است — پس هرکسی با یک شناسه‌ی پروژه می‌توانست کار را به نام پروژه‌ی
تنانت دیگری ثبت کند. حالا از `Project::ownedBy` رد می‌شود.

**پروژه‌ی تنانت دیگر، ۴۰۴ می‌گیرد نه ۴۰۳.** ۴۰۳ تأیید می‌کند که آن پروژه وجود
دارد و مال کس دیگری است — خودش یک افشای اطلاعات است.

**Sanctum فقط توکن، بدون session.** مقدار پیش‌فرض `guard => ['web']` باعث
می‌شود اول session چک شود و بعد توکن. روی یک API این دو جور غلط است: یک کوکی
می‌تواند درخواست API را احراز هویت کند (و CSRF را برمی‌گرداند)، و ابطال توکن
درخواستی را که با session احراز شده متوقف نمی‌کند.

**تست‌های گیت‌وی روی Postgres واقعی اجرا می‌شوند، نه SQLite.** گیت‌وی حالا به
چیزهایی وابسته است که SQLite ندارد: کلید UUID، `CITEXT` برای یکتایی ایمیل
بدون حساسیت به حروف، و کلید خارجی به جدول‌هایی که سرویس‌های پایتون می‌نویسند.
تست روی موتور دیگری یعنی دقیقاً همان حالت‌های جالب — `Ali@` در برابر `ali@` —
در تست سبز و در واقعیت قرمز باشند.

**Orchestrator اولین مصرف‌کننده است.** تا قبل از این، هر رویدادی که ساخته شده
بود تولیدکننده داشت و هیچ مصرف‌کننده‌ای نداشت — یعنی کل باس توجیه‌نشده بود.
گردش‌کار `site_audit` رویداد `crawl.requested` را می‌فرستد، منتظر
`crawl.completed` می‌ماند، بعد `keyword.research_requested` را می‌فرستد، و در
آخر `workflow.completed` را منتشر می‌کند. Orchestrator هیچ‌وقت سرویسی را با
HTTP صدا نمی‌زند.

**برنامه‌ریز قطعی است، نه مدل زبانی.** معماری اسم این را «ایجنت planner»
گذاشته، ولی یک plan تصمیم درباره‌ی جریان کنترل است، و جریان کنترلی که هر بار
فرق کند قابل دیباگ نیست. قانون‌ها شکل مراحل را تعیین می‌کنند؛ کار مدل — وقتی
اضافه شود — پر کردن پارامترهاست نه اختراع مرحله. این باعث می‌شود کل
Orchestrator بدون هیچ API key کار کند، همان تصمیمی که برای پیشنهادهای محتوا
هم گرفته شد.

**شناسه‌ی کار را Orchestrator از قبل می‌سازد، نه سرویس.** `crawl_id` قبل از
ارسال ساخته و روی مرحله ذخیره می‌شود؛ به همین دلیل `crawl.completed` می‌تواند
به مرحله‌ای که درخواستش کرده برگردد. تکیه بر `correlation_id` به تنهایی جواب
نمی‌داد اگر سرویسی شناسه‌ی خودش را انتخاب می‌کرد.

**پیشروی زیر قفل ردیف انجام می‌شود.** دو رویداد تکمیل می‌توانند هم‌زمان روی دو
نمونه‌ی Orchestrator بنشینند؛ بدون `FOR UPDATE` هر دو همان مرحله‌ی pending را
می‌دیدند و هر دو می‌فرستادندش — یعنی خزش دو بار اجرا می‌شد. تستش دو نخ را با
`threading.Barrier` هم‌زمان می‌کند.

**رویداد تکراری، یک بار پیش می‌برد.** گذار مرحله `dispatched → completed` است و
با یک UPDATE شرطی انجام می‌شود، پس تحویل دوم هیچ ردیفی را نمی‌گیرد.

**رویداد تکمیلی که به هیچ گردش‌کاری تعلق ندارد، خطا نیست.** خزش‌هایی که مستقیم
از API شروع می‌شوند همان `crawl.completed` را منتشر می‌کنند؛ خطا حساب کردنشان
یعنی dead-letter کردن پیام‌های کاملاً سالم.

**Orchestrator حالت بدون دیتابیس ندارد.** بازگشت به فایل برای سرویس‌ها معنی
دارد چون یک job یک نتیجه است؛ ولی یک گردش‌کار چند سرویس را در طول چند دقیقه
هماهنگ می‌کند و «وضعیت را در همین پروسه نگه دار» حالتی نیست که بشود صادقانه
پیشنهاد داد. نبود `DATABASE_URL` خطای بالا آمدن است، نه تنزل بی‌صدا.

**مرز چیزی که تأیید نشده، صریح است.** همه‌ی موتورهای جستجو از این محیط بلاک
هستند، پس `services/serp/providers.py` اجرا نشده. به‌جای پنهان کردنش، fetch
پشت یک اینترفیس جدا شد: سطح تأییدنشده یک تابع است، و هر چیزی که نتیجه را
مصرف می‌کند — رتبه‌یابی، رقبا، اولویت‌بندی، ذخیره‌سازی، رویدادها، هماهنگی —
روی داده‌ای با شکل معلوم تست شده. تست parser روی fixture دست‌نویس اجرا می‌شود
و خودش می‌گوید که «شکل را پوشش می‌دهد، نه اینکه شکل امروزی است».

**عدد تخمینی به‌عنوان پیش‌بینی گزارش نمی‌شود.** جدول CTR فقط برای مرتب کردن
فرصت‌ها نسبت به هم استفاده می‌شود، نه پیش‌بینی ترافیک. یک تخمین که لباس
پیش‌بینی بپوشد از نداشتن عدد بدتر است.

**«رتبه ندارد» با «رتبه صفر» یکی نیست.** ستون `position` می‌تواند NULL باشد و
میانگین رتبه کلمات بدون رتبه را کنار می‌گذارد. اگر عدد بزرگی به‌جایش گذاشته
می‌شد، داده‌ای ساخته بودیم که وجود ندارد.

**زیردامنه خودی است، دامنه‌ی شبیه نه.** `blog.example.com` با
`example.com` یکی حساب می‌شود ولی `notexample.com` نه — تطبیق پسوندی خالی
رتبه‌ی رقیب را به اسم خودت گزارش می‌کرد.

**مرحله‌ی سوم واقعاً به مرحله‌ی دوم وابسته است.** چیزی که رتبه‌اش چک می‌شود
همان کلماتی است که تحقیق پیدا کرده، پس این مرحله را نمی‌شود از قبل برنامه‌ریزی
کرد. این قوی‌ترین دلیل وجود Orchestrator در کل سیستم است.

**«چیزی برای انجام نیست» با «خراب شد» فرق دارد.** اگر تحقیق هیچ کلمه‌ای پیدا
نکند، مرحله‌ی SERP `skipped` می‌شود و گردش‌کار `completed` — نه `failed`.
سایتی که کلمه‌ای ندارد یک جواب واقعی درباره‌ی آن سایت است؛ شکست دادن
گردش‌کار یعنی دور ریختن نتیجه‌ی خزش هم.

**بازگشت به فایل، بی‌صدا نیست.** اگر `DATABASE_URL` ست باشد ولی دیتابیس در
دسترس نباشد، سرویس بالا می‌آید و روی فایل کار می‌کند — ولی با لاگ سطح خطا.
نصب تک‌ماشینه‌ی توی README دیتابیس ندارد و باید کار کند؛ ولی از دست دادن
بی‌صدای outbox خیلی بدتر از از دست دادن پرصدایش است.

**خواندن هم تنانت دارد، نه فقط نوشتن.** این بزرگ‌ترین حفره‌ای بود که تا حالا
پیدا شد و موقع خواندن کد گیت‌وی برای ساختن UI روی آن پیدا شد، نه با تست:
نوشتن با کلید خارجی به تنانت مقید بود، ولی **خواندن هیچ فیلتری نداشت**. یعنی
یک شناسه‌ی گردش‌کار برای دیدن کل ممیزی یک حساب دیگر کافی بود — امتیاز، کلمات
کلیدی، جایگاه‌ها — و `GET /v1/workflows` گردش‌کار همه‌ی تنانت‌ها را برمی‌گرداند.
همین برای crawls، research و checks هم برقرار بود.

حالا هر endpoint خواندن `tenant_id` می‌گیرد و فیلتر در خود SQL انجام می‌شود، نه
بعد از لود شدن ردیف. در گیت‌وی این پارامتر **اجباری** است، نه اختیاری: نسخه‌ای
که اختیاری بود همان نسخه‌ای است که این باگ را داشت، و یک آرگومان اجباری را
نمی‌شود فراموش کرد (کامپایل نمی‌شود). کاربرِ بدون تنانت هم به‌جای «همه چیز»،
هیچ‌چیز می‌بیند — همان ترفند صفرِ UUID که `Project::ownedBy` استفاده می‌کند.
پاسخ به شناسه‌ی تنانت دیگر ۴۰۴ است نه ۴۰۳، چون ۴۰۳ خودش تأیید می‌کند که آن
شناسه وجود دارد. روی گیت‌وی و ارکستریتور واقعی با دو حساب واقعی امتحان شد.

**شکل ذخیره‌سازی با شکل API یکی نیست.** همان تغییر بالا یک باگ دوم را بیرون
انداخت: فایل‌ها از `to_dict` نوشته می‌شدند و هر سرویس `to_dict` را برای payload
عمومی‌اش override کرده بود — هیچ‌کدام `tenant_id` را نداشتند. رکورد از حافظه
درست جواب می‌داد و از دیسک مالکش را گم می‌کرد، پس لیست یک تنانت با اولین
ری‌استارت پروسه خالی می‌شد. حالا `to_storage` جداست و از `asdict` می‌آید، پس
نمی‌تواند از فیلدهای واقعی عقب بماند.

**۴۰۱ برای درخواست بدون توکن، حتی بدون هدر Accept.** این هم با curl زدن به
گیت‌واه در حال اجرا پیدا شد نه با تست: لاراول کاربر مهمان را به مسیر `login`
ریدایرکت می‌کند، این اپ اصلاً چنین مسیری ندارد، و نتیجه ۵۰۰ بود. تست‌ها
نمی‌دیدندش چون `getJson()` هدر Accept می‌فرستد و آن مسیر اصلاً به ریدایرکت
نمی‌رسد.

**مدل زبانی یک در دارد، نه چهار تا.** هر تماس با Claude از `shared/llm` رد
می‌شود. دلیلش تمیزی نیست: تصمیم اینکه «امتناع مدل» یا «پاسخ نصفه‌مانده» یا
«متن به‌جای JSON» یعنی چه، باید یک‌بار گرفته شود. موتور سئو نسخه‌ی دوم خودش را
داشت و همان‌جا حذف شد. بیرونِ این دروازه هیچ‌کس استثنای SDK نمی‌بیند؛ فقط
`AIUnavailable` می‌بیند و مسیر قانون‌محورش را می‌رود.

**خلاصه دو لایه است و لایه‌ی اول همیشه هست.** خلاصه‌ی قانون‌محور یک تابع خالص
از گزارش است و داخل همان تراکنشی نوشته می‌شود که گردش‌کار را تمام می‌کند، پس
هر گزارشی خلاصه دارد — با کلید API یا بدونش. لایه‌ی دوم (نوشته‌ی مدل) بعد از
آن و **بیرون از قفل** اجرا می‌شود، از روی رویداد `workflow.completed` که ورکر
از خودش می‌گیرد. این حلقه نیست: هندلرش چیزی منتشر نمی‌کند. دلیل این جداسازی
ساده است — یک تماس سی‌ثانیه‌ای داخل تراکنشی که روی ردیف گردش‌کار قفل دارد،
هر رویداد بعدی همان گردش‌کار را پشت خودش صف می‌کند.

**اگر مدل جواب ندهد، گزارش عوض نمی‌شود.** نبود کلید، امتناع، تایم‌اوت، پاسخ
غیرقابل‌تجزیه، حتی پاسخ معتبرِ خالی — همه به یک چیز ختم می‌شوند: خلاصه‌ی
قانون‌محور سر جایش می‌ماند و دلیلش در `summary.note` نوشته می‌شود. `enrich`
هیچ‌وقت استثنا پرت نمی‌کند، چون خلاصه آخرین کار یک گردش‌کارِ تمام‌شده است و
خرابی آن نباید یک ممیزی موفق را تبدیل به شکست کند.

**گراف لینک از خزش می‌آید، نه از خزش دوم.** گزارش خزش حالا یال‌های داخلی را
هم حمل می‌کند. تا قبل از این، فهرست صفحات می‌گفت هر صفحه چند لینک ورودی دارد
ولی نمی‌گفت **از کجا** — و هر سؤالی که درباره‌ی لینک‌سازی داخلی ارزش پرسیدن
دارد، سؤالی درباره‌ی یال‌هاست. سقفش پنجاه هزار یال است با پرچم `truncated`:
گراف ناقصی که نگوید ناقص است، جواب غلط درباره‌ی صفحات یتیم می‌دهد.

**تنها تماس سرویس‌به‌سرویس پروژه، عمدی است.** سرویس Internal Links گزارش خزش
را با HTTP از سرویس Crawl می‌گیرد. بقیه‌ی چیزها روی باس رد می‌شوند چون خلاصه‌اند؛
یک گزارش خزش خلاصه نیست و از اول به همین دلیل `result_url` روی رویداد بود.
`tenant_id` هم با آن درخواست می‌رود، وگرنه همان حفره‌ای که در گیت‌وی بسته شد یک
لایه پایین‌تر باز می‌ماند.

**مرحله‌ی چهارم آخر است، نه دوم.** تحلیل لینک فقط به خزش نیاز دارد و می‌توانست
دوم باشد؛ ولی حساب‌وکتاب روی داده‌ی موجود است و هزینه‌ی شبکه ندارد، پس گذاشتنش
در انتها باعث می‌شود مراحل گران‌تر زودتر شروع شوند.

**«هر دوشنبه ساعت ۹» سه جواب غلط دارد.** ساعت ۹ در کدام منطقه‌ی زمانی —
منطقه ذخیره می‌شود و حساب در همان انجام می‌شود، وگرنه با تغییر ساعت یک ساعت
جابه‌جا می‌شود. ۳۱ در ماهی که ۳۰ روز دارد — به آخر ماه چسبانده می‌شود، چون
«۳۱اُم» از زبان کسی که گزارش ماهانه تنظیم می‌کند یعنی «آخر ماه». و ماشینی که
یک هفته خاموش بوده — به اولین اجرای آینده می‌پرد، نه هفت اجرای پشت سر هم؛ هفت
گزارش یکسان یعنی صندوق پر و هفت بار خزیدن سایت یک نفر.

**زمان‌بند ساده‌ترین پروسه‌ی سیستم است.** با باس حرف نمی‌زند و نمی‌داند
گردش‌کار چیست: از دیتابیس می‌پرسد چه چیزی سررسیده، در outbox می‌گذارد، و
می‌خوابد. رله بقیه‌اش را می‌برد، دقیقاً مثل هر تولیدکننده‌ی دیگر. شلیک و
جابه‌جا کردن `next_run_at` در یک تراکنش انجام می‌شود و ردیف با
`FOR UPDATE SKIP LOCKED` گرفته می‌شود، پس اجرای دو زمان‌بند هم‌زمان بی‌خطر
است.

**زمان‌بندی به‌جای cron نوشته شد.** یک رشته‌ی cron نه می‌تواند بگوید «۳۱ یعنی
آخر ماه» و نه منطقه‌ی زمانی‌ای را که ساعت در آن معنی دارد حمل کند — و همین دو
تفاوت بین زمان‌بندی‌ای است که سر قولش می‌ماند و یکی که آرام‌آرام جابه‌جا
می‌شود.

**یک‌بار فرستادن با ایندکس یکتا، نه با حافظه.** تحویل at-least-once است، پس
همان رویداد دوبار می‌رسد. جفت `(channel_id, event_id)` قید یکتا دارد: ورکر اول
INSERT می‌کند و دومی conflict می‌گیرد. هر راه‌حل درون‌حافظه‌ای با اولین
ری‌استارت یعنی دو ایمیل درباره‌ی یک گزارش — و اعلان، برخلاف هر کار دیگری در این
سیستم، پس گرفته نمی‌شود.

**آدرس وبهوک خطرناک‌ترین آدرس کل پلتفرم است.** کاربر تایپش می‌کند و سروری که
داخل شبکه‌ی خصوصی نشسته آن را می‌زند. از همان SSRF guard رد می‌شود که خزنده
استفاده می‌کند — هم موقع ساخت کانال و هم موقع ارسال — و به همین مناسبت guard
از `services/engine` به `shared/netguard.py` منتقل شد، چون حالا سه سرویس به
آدرس تایپ‌شده‌ی کاربر درخواست می‌زنند. آدرسی که resolve نشود هم رد می‌شود:
guard نمی‌تواند ثابت کند نام ناشناخته داخلی نیست.

**امضا روی همان بایت‌های ارسالی است.** بدنه یک‌بار سریال می‌شود و HMAC روی
همان بایت‌ها؛ امضای روی سریال‌سازی دوباره‌ی دیکشنری چیزی است که گیرنده
نمی‌تواند بازتولید کند. در اجرای واقعی، گیرنده امضا را مستقل محاسبه کرد و
درست بود.

**راز یک‌بار نشان داده می‌شود و بس.** فهرست کانال‌ها راز امضا را برنمی‌گرداند و
آدرس ایمیل در فهرست تحویل‌ها ماسک می‌شود — فهرست همان چیزی است که سر از
اسکرین‌شات درمی‌آورد.

**سند نهایی خودبسنده است.** گزارش HTML هیچ درخواستی به بیرون ندارد — نه فونت
از CDN، نه استایل‌شیت، نه اسکریپت. گزارشی که استایلش را از اینترنت می‌گیرد،
شش ماه بعد آفلاین برای کسی که ایمیلش کرده‌اند خراب باز می‌شود، و دقیقاً
همین‌طور خوانده می‌شود. با Chromium واقعی رندر شد و شمارنده‌ی درخواست‌های
بیرونی صفر بود.

**PDF یعنی چاپ همین HTML.** موتور PDF سمت سرور اضافه نشد؛ به‌جایش قواعد چاپ
(`@page A4`، `break-inside`) داخل خود فایل‌اند. اینجا با headless Chromium
یک PDF دویست‌کیلوبایتی از همان فایل گرفته شد. یک وابستگی سنگین کمتر، و فایلی
که همه‌جا باز می‌شود.

**هر متن قبل از رفتن به سند escape می‌شود.** عنوان صفحه از سایت یک نفر دیگر
می‌آید؛ گزارشی که آن را خام رندر کند راهی است برای اینکه صفحه‌ی خزیده‌شده به
مرورگر خواننده‌ی گزارش حمله کند. تست‌ها همین را می‌سنجند.

**دومین مصرف‌کننده‌ی `workflow.completed`.** ورکر ارکستریتور همان رویداد را
برای خلاصه‌ی مدل می‌گیرد و سرویس Reporting برای ساخت سند. هیچ‌کدام از دیگری
خبر ندارد و ارکستریتور اصلاً نمی‌داند این سرویس وجود دارد — همین fan-out
دلیل وجود باس است. هر گردش‌کار **یک** سند خودکار می‌گیرد؛ درخواست صریح همیشه
دوباره رندر می‌کند، که راه برداشتن خلاصه‌ی مدلی است که دیرتر رسیده.

**اولین سرویسی که پیشنهاد می‌دهد، نه گزارش.** تا اینجا همه‌ی سرویس‌ها چیزی را
اندازه می‌گرفتند. سرویس Optimizer متن جایگزین پیشنهاد می‌دهد، و همین معنی «بدون کلید
API هم کار می‌کند» را عوض می‌کند: لایه‌ی قانون تصمیم می‌گیرد **کدام صفحه، چه
چیزش، و هر اصلاح چه شرطی دارد** — عبارت هدف، بازه‌ی طول، اینکه کلیدواژه باید
بیاید یا نه — و هر جا بشود بدون ساختن چیزی کاندیدا می‌دهد (کوتاه کردن عنوان سر
مرز کلمه، برداشتن H1 صفحه به‌عنوان عنوان). جایی که کاندیدا دادن یعنی نوشتن
متن، نمی‌نویسد. بدون کلید، خروجی یک دستورالعمل دقیق است نه جمله‌ای ساختگی.

**پاسخ مدل با همان شرط‌هایی که قانون گذاشته سنجیده می‌شود.** بازنویسی‌ای که از
بازه‌ی طول بیرون بزند، عبارت هدف را نداشته باشد یا خالی باشد دور ریخته می‌شود و
کاندیدای قانونی می‌ماند. مدل نویسنده‌ی بهتری است؛ داور بهتری برای محدودیت‌ها
نیست. `written_by` در گزارش و در ستون دیتابیس می‌گوید کدام حالت اتفاق افتاده،
چون سؤالی که آدم از یک فهرست می‌پرسد همین است.

**عنوان بدون عبارت هدف کاندیدای مکانیکی نمی‌گیرد.** چسباندن کلیدواژه دقیقاً
همان عنوان اسپم‌شده‌ای را می‌سازد که این ابزار قرار است علیهش استدلال کند.

**روند، چیزی است که زمان‌بندی را از انبار گزارش به یک سری تبدیل می‌کند.**
گزارشی که هر هفته همان عددهای مطلق را تکرار کند به سؤالی جواب می‌دهد که خواننده
از قبل جوابش را دارد؛ سؤال واقعی از اجرای دوم به بعد این است که «بهتر شده یا
نه». چهار قاعده‌اش همه از یک جنس‌اند — چیزهایی که در پیاده‌سازی بدیهی غلط
درمی‌آیند و درست به‌نظر می‌رسند:

- **علامت، جهت نیست.** بالا رفتن امتیاز خوب است و بالا رفتن تعداد ایراد بد.
  «۳+» به‌تنهایی خواننده را مجبور می‌کند یادش باشد کدام سنجه کدام است.
- **نبودن، صفر نیست.** اگر بررسی جایگاه هفته‌ی قبل اجرا نشده باشد، «۰ ← ۵»
  ادعایی درباره‌ی سایت است در حالی که واقعیت فقط این است که آن مرحله این بار
  اجرا شد. هر طرف که نداشته باشد، آن سنجه اصلاً مقایسه نمی‌شود.
- **جابه‌جایی کوچک خبر نیست.** ۷۱ در برابر ۷۲ همان سایت است. گزارش کردنش به
  خواننده یاد می‌دهد این بخش را نخواند.
- **خزش با اندازه‌ی متفاوت، نمونه‌ی متفاوت است.** سنجه‌های شمارشی با تعداد صفحه
  بالا و پایین می‌روند، پس وقتی دو خزش هم‌اندازه نبودند کنار گذاشته می‌شوند و
  همین در گزارش نوشته می‌شود، نه اینکه نبودشان «بدون تغییر» خوانده شود.

**امتیاز دسته‌ها هم مقایسه می‌شود، و دلیلش یک اجرای واقعی بود.** سایتی که روی هر
صفحه توضیح متا گرفت و طول محتوایش سه برابر شد، «تغییر معناداری نیست» گرفت —
چون امتیاز کلی و تعداد ایراد اتفاقاً همان‌جا ماندند، در حالی که امتیاز «محتوا و
کلمات کلیدی» از ۶۸ به ۷۶ رفته بود. کسی که یک هفته کار کند و این را بخواند
نتیجه می‌گیرد ابزار خراب است.

**فن‌اوت یک مرحله به ازای هر رقیب است، نه یک مرحله که باز می‌شود.** ماشین حالت
از قبل بلد است یک مرحله را اعزام کند و منتظر رویدادش بماند؛ مرحله‌ای که N کار
تولید کند به نوع دومی از انتظار نیاز داشت — قفل خودش، قاعده‌ی شکست جزئی خودش،
باگ‌های خودش. با یک مرحله به ازای هر رقیب، تنها چیزی که واقعاً عوض شد این بود
که پارامترهای مرحله باید ذخیره شوند: تا حالا هر مرحله پارامترش از ورودی
گردش‌کار درمی‌آمد، و خزش رقیب اولین مرحله‌ای است که `start_url` خودش را دارد.

**خزش رقیب اگر شکست بخورد، ممیزی شما شکست نمی‌خورد.** این تنها نوع مرحله‌ای است
که در `OPTIONAL_KINDS` نشسته. سایت رقیب که پایین باشد چیزی درباره‌ی سایت شما
نمی‌گوید، و از دست دادن کل ممیزی به‌خاطرش مقایسه را از یک امکان به یک ریسک
تبدیل می‌کرد. خود مرحله در گزارش `failed` می‌ماند؛ گردش‌کار نه.

**«خزش» در بقیه‌ی مراحل همیشه یعنی خزش شما.** یک گردش‌کار حالا می‌تواند پنج خزش
داشته باشد. اگر `_own_crawl` نبود، گراف لینک و پوشش محتوا و پیشنهاد بازنویسی
همگی بی‌صدا سایت یک رقیب را توصیف می‌کردند — تستش هم دقیقاً همین است.

**نمونه، سایت نیست — و مقایسه‌ی رقبا روی همین یک جمله بنا شده.** خزش سقف دارد.
مقایسه‌ی «۲۰ صفحه» با «۲۰ صفحه» نمی‌گوید دو سایت هم‌اندازه‌اند، فقط می‌گوید سقف
یکی بوده. برای همین در سرویس Competitor هیچ سنجه‌ای مجموع نیست: همه نرخ یا
میانه‌اند، و اندازه‌ی نمونه داخل گزارش می‌آید. سایتی که نمونه‌اش برای معنا داشتن
کوچک است در `ignored` **نام برده می‌شود** نه اینکه بی‌صدا حذف شود؛ حذف بی‌صدا
گزارش را محکم‌تر از آنچه هست نشان می‌دهد. و یک عبارت وقتی «موضوع» است که دست‌کم
دو رقیب رویش نوشته باشند، وگرنه سبک نوشتن یک سایت به‌عنوان شکاف سایت شما گزارش
می‌شود.

**معیار، میانه‌ی رقباست نه بهترینشان.** یک رقیب که در یک چیز فوق‌العاده است
استاندارد نیست. دنبال کردن بیشینه در همه‌ی سنجه‌ها همان‌جایی است که گزارش مقایسه
به فهرست تمام‌نشدنی تبدیل می‌شود. به همان دلیل، «جلوترید» هم گزارش می‌شود، نه
فقط «عقب‌اید»: گزارشی که فقط کمبود می‌شمارد نمی‌گوید چه چیزی را باید حفظ کرد.

**املای فارسی یک جا تعریف شده، چون دو جا یعنی دو جواب.** جدول تبدیل («ي» عربی
به «ی»، نیم‌فاصله، اعراب، کشیده) از سرویس Content به `shared/text.py` منتقل شد
وقتی سرویس Competitor به همان قاعده نیاز پیدا کرد. اعداد «عنوان خوب چند
کاراکتر است» هم همین‌طور: `shared/seo.py`. سه نسخه از «۲۵ تا ۶۰» یعنی سه جا که
می‌توانند با هم اختلاف پیدا کنند، و گزارشی که صفحه‌ای را جور دیگری نمره می‌دهد
تا سرویسی که بازنویسی‌اش را پیشنهاد می‌کند، گزارشی است که کسی باورش نمی‌کند.

**پوشش محتوا تنها چیزی است که به دو مرحله همزمان نیاز دارد.** خزش می‌داند چه
صفحه‌هایی وجود دارد، تحقیق کلمات کلیدی می‌داند مردم دنبال چه می‌گردند، و سؤال
«برای کدام عبارت صفحه نداریم» فقط از کنار هم گذاشتن این دو جواب می‌گیرد. این
قوی‌ترین دلیل وجود ارکستریتور بعد از مرحله‌ی SERP است: یک قانون داخل موتور
هرگز نمی‌تواند این را ببیند، چون موتور اصلاً تحقیق کلمات کلیدی را نمی‌بیند.

**رقابت داخلی چیزی است که ممیزی صفحه‌به‌صفحه هرگز پیدا نمی‌کند.** وقتی سه صفحه
یک عبارت را هدف می‌گیرند، سیگنال بینشان تقسیم می‌شود و هر سه بدتر از یکی رتبه
می‌گیرند — ولی هر کدام به‌تنهایی کاملاً سالم به نظر می‌رسند. فقط نگه داشتن کل
سایت و فهرست کلمات با هم این را نشان می‌دهد.

**تطبیق روی اعلام صفحه است، نه متن بدنه؛ و همین در گزارش نوشته می‌شود.** عنوان،
توضیح متا و تیترها. متن بدنه یعنی کل خزش دوباره در حافظه، و صفحه‌ای که یک بار
عبارتی را در پاراگرافی آورده آن را هدف نگرفته. این محدودیت داخل خود گزارش
(`method`) می‌آید تا کسی بعداً کشفش نکند.

**فارسی یک املا دارد.** «ي» عربی به «ی»، «ك» به «ک»، نیم‌فاصله مرز کلمه، اعراب
و کشیده حذف. بدون این، صفحه‌ای با عنوان «کفش ورزشي» با کلیدواژه‌ی «کفش ورزشی»
جور نمی‌شد و گزارش کاری را که انجام شده «انجام‌نشده» می‌گفت. در اجرای واقعی
همین اتفاق تأیید شد.

**داشبورد سرور ندارد.** فرانت‌اند فقط در مرورگر اجرا می‌شود (`ssr: false`).
گیت‌وی با توکن Bearer احراز هویت می‌کند نه کوکی؛ رندر سمت سرور یعنی آن سرور
باید توکن هر بازدیدکننده را نگه دارد تا به‌جای او درخواست بزند — یک جای دوم
برای زندگی توکن‌ها که دلیلی برای وجودش نیست. توکن هم در `sessionStorage`
می‌ماند، نه `localStorage`: چون Bearer است گزینه‌ی HttpOnly بدون یک سرور واسط
وجود ندارد، و بین این دو، آن که با بسته شدن تب می‌میرد پنجره‌ی کوچک‌تری است.

**نگهبان مسیر در فرانت یک راحتی است، نه یک کنترل.** هر صفحه داده‌اش را از
گیت‌وی می‌گیرد و گیت‌وی خودش توکن را بررسی می‌کند. کدی که در دست کاربر اجرا
می‌شود از هیچ‌چیز محافظت نمی‌کند؛ فقط جلوی نشان دادن صفحه‌ی خالی‌ای را می‌گیرد
که قرار است ۴۰۱ بگیرد.

**قضاوت‌های UI در توابع خالص است، نه در قالب‌ها.** «جایگاه خالی یعنی چه»،
«کِی polling باید بایستد»، «هر کد وضعیت گیت‌وی به کاربر چه می‌گوید» — همه در
`app/utils/format.ts` و بدون مرورگر تست‌شده. مهم‌ترینشان همان قاعده‌ی همیشگی
است: جایگاه `null` یعنی «در نتایجی که بررسی شد پیدا نشد»، و هیچ عددی به‌جایش
چاپ نمی‌شود.

**ساعت زمان‌بندی به وقت خودِ زمان‌بندی نشان داده می‌شود.** `next_run_at` به UTC
می‌آید و مرورگر هرجایی که کاربر نشسته است؛ اگر با ساعت محلی مرورگر رندر شود،
کسی که «۹ صبح تهران» را انتخاب کرده روی صفحه ۰۵:۳۰ می‌بیند و نتیجه می‌گیرد
زمان‌بندی خراب است. تقویم هم صریحاً میلادی است — scheduler هیچ حساب جلالی
نمی‌کند و چاپ تاریخ شمسی کنار «روز ۳۱ هر ماه» یعنی گفتن چیزی که درست نیست.
اسم روزها هم از خود Intl گرفته می‌شود، نه دستی، تا جمله‌ی دوره و تاریخ کنارش
نتوانند یک روز را دوجور بنویسند.

**تماس زنده تأیید نشده است.** این محیط `ANTHROPIC_API_KEY` ندارد، پس هیچ
درخواستی به Claude ارسال نشده. همان کاری که با provider سرویس SERP شد اینجا
هم تکرار شد: تنها سطح تأییدنشده یک تماس HTTP است و بقیه — پارامترهای درخواست،
و رفتار در برابر هر شکل پاسخ (امتناع، سقف توکن، خالی، متن ساده، لیست به‌جای
شیء) — با کلاینت بدلی تست شده‌اند. اولین اجرای واقعی، مرحله‌ی تأیید است.

## اجرای گیت‌وی

گیت‌وی مهاجرت خودش را ندارد؛ اسکیما از فایل‌های SQL می‌آید:

```bash
createdb seo
DATABASE_URL=postgresql://seo@127.0.0.1/seo infra/db/migrate.sh

cd apps/api-gateway
composer install
cp .env.example .env && php artisan key:generate     # DB_URL و REDIS_HOST را تنظیم کن
TEST_DATABASE_URL=postgresql://seo@127.0.0.1/seo php artisan test   # ۱۵۰ تست
php artisan serve
```

بدون `TEST_DATABASE_URL` کل سوئیت skip می‌شود — همان انضباط `shared/tests`.

مسیر کامل یک کاربر:

```bash
curl -X POST localhost:8000/api/v1/auth/register \
  -d '{"name":"...","email":"...","password":"...","tenant_name":"..."}'
# → {"user": {...}, "token": "..."}   تنانت ساخته شد، سرویس‌ها می‌شناسندش
curl -X POST localhost:8000/api/v1/projects   -H "Authorization: Bearer $TOKEN" ...
curl -X POST localhost:8000/api/v1/research   -H "Authorization: Bearer $TOKEN" ...
```

## اجرای حلقه‌ی کامل

این دقیقاً همان چیزی است که اینجا اجرا شد (Postgres، RabbitMQ و چهار پروسه‌ی
واقعی):

```bash
python -m services.crawl.worker &
python -m services.keyword.worker &
python -m agents.orchestrator.worker &
python -m agents.orchestrator.scheduler &   # اگر زمان‌بندی می‌خواهی
uvicorn services.competitor.api:app --port 8010 &   # مقایسه با رقبا
python -m shared.relay &
uvicorn agents.orchestrator.api:app --port 8200

curl -X POST localhost:8200/v1/workflows \
  -d '{"goal":"site_audit","inputs":{"start_url":"https://example.com"}}'
```

ترتیب رویدادهایی که در `outbox` ثبت شد:

```
crawl.requested             orchestrator
crawl.completed             crawl-service
page.updated  ×۲            crawl-service
keyword.research_requested  orchestrator      ← فقط بعد از تمام شدن خزش
keyword.researched          keyword-service
serp.check_requested        orchestrator      ← کلماتش از نتیجه‌ی مرحله‌ی قبل
serp.checked                serp-service
links.analysis_requested    orchestrator      ← فقط شناسه‌ی خزش را حمل می‌کند
links.analyzed              internal-links-service
content.analysis_requested  orchestrator      ← شناسه‌ی خزش + شناسه‌ی تحقیق
content.analyzed            content-service
optimizer.plan_requested    orchestrator      ← تحقیق اینجا اختیاری است
optimizer.planned           optimizer-service
workflow.completed          orchestrator      ← دو مصرف‌کننده: خلاصه و سند
report.rendered             reporting-service ← notifications هم می‌گیردش
```

آخرین رویداد به همان ورکری برمی‌گردد که منتشرش کرده، تا خلاصه‌ی گزارش را
بنویسد. اینجا بدون کلید API اجرا شد و لاگ دقیقاً همین را گفت:

```
workflow … keeps its rule-based summary: ANTHROPIC_API_KEY تنظیم نشده…
```

گزارش نهایی: امتیاز ۴۵ روی ۲ صفحه با ۱۶ ایراد، ۲۰۰ کلمه‌ی کلیدی در ۱۲ خوشه،
۱۰ کلمه رتبه‌یابی‌شده با میانگین رتبه‌ی ۳. (مرحله‌ی SERP با provider جایگزین
اجرا شد، چون موتورهای جستجو از اینجا بلاک‌اند.)

## اجرای داشبورد

```bash
cd apps/web-dashboard
npm install --legacy-peer-deps
npm test                                   # ۳۵ تست
npm run build
NUXT_PUBLIC_API_BASE=http://127.0.0.1:8000/api node .output/server/index.mjs
```

اینجا با Chromium واقعی روی کل پشته درایو شد — Postgres، RabbitMQ، رله، سه
ورکر، ارکستریتور، گیت‌وی و خود داشبورد. مسیری که در مرورگر طی شد: ثبت‌نام →
گرفتن توکن → شروع تحلیل روی یک سایت آزمایشی محلی → صفحه‌ی گزارش که خودش تازه
می‌شد تا وضعیت به «انجام شد» رسید → ساخت پروژه.

آنچه در آن اجرا دیده شد و درست هم بود: تحقیق کلمات کلیدی صفر کلمه برگرداند
(autocomplete گوگل از این محیط ۴۰۳ می‌دهد)، پس مرحله‌ی سوم `skipped` شد و
گردش‌کار `completed` ماند نه `failed` — همان تصمیمی که بالا نوشته شده، این بار
در مرورگر. مرحله‌ی SERP هم با یک provider جایگزین در همان پروسه اجرا شد، چون
موتورهای جستجو بلاک‌اند؛ آن اسکریپت در مخزن نیست.

یک ایراد از همان اجرا بیرون آمد و اصلاح شد: خلاصه‌ی قانون‌محور مرحله‌ی
رد‌شده را با نام داخلی‌اش («serp_check») می‌نوشت، نه با نامی که کاربر
می‌شناسد.

### صفحه‌های زمان‌بندی و اطلاع‌رسانی

این دو هم با همان روش روی گیت‌وی واقعی درایو شدند (ارکستریتور، سرویس
اطلاع‌رسانی، گیت‌وی لاراول، Postgres). آنچه در مرورگر انجام شد: ساخت یک
زمان‌بندی هفتگی با منطقه‌ی زمانی `Europe/Berlin` و خواندن «اجرای بعدی» به همان
وقت (۰۷:۰۰ برلین از `05:00Z`)، توقف و ادامه و حذف همان ردیف، ساخت کانال ایمیل
و کانال وبهوک، دیدن کلید امضا و ناپدید شدنش بعد از reload، و آزمایش ارسال روی
کانال ایمیل که بدون SMTP پیکربندی‌شده «SMTP_HOST is not configured» را روی صفحه
نشان داد — همان چیزی که سرویس واقعاً برگردانده بود.

دو ایراد از همین اجرا بیرون آمد و اصلاح شد. یکی: نام روز در جمله‌ی دوره
(«هر پنج‌شنبه») با نام روز در ستون اجرای بعدی («پنجشنبه» از Intl) یکی نبود —
حالا هر دو از Intl می‌آیند تا نتوانند اختلاف پیدا کنند. دوم: فرستنده‌ی وبهوک
وقتی چیزی جز کد وضعیت نمی‌داند `error` را «HTTP 503» می‌نویسد، و صفحه آن را
کنار خود کد چاپ می‌کرد: «HTTP 503 — HTTP 503».

## اجرای مقایسه‌ی رقبا

سه سایت کوچک محلی روی سه پورت بالا آمدند و عمداً نابرابر ساخته شدند: مال «من»
با عنوان‌های ناقص، بدون توضیح متا و صفحه‌های ۸۲ کلمه‌ای؛ دو رقیب با عنوان‌های
کامل، توضیح متا روی همه‌ی صفحه‌ها، متن ۳۲۳ کلمه‌ای و یک موضوع («ماراتن») که سایت
من اصلاً درباره‌اش ننوشته. هر سه با سرویس Crawl واقعی از طریق گیت‌وی و با توکن
واقعی خزیده شدند، بعد `POST /v1/comparisons`.

خروجی: `title_in_range` صفر در برابر ۸۵٫۷، `described` صفر در برابر ۱۰۰،
`thin` صد در برابر صفر — هر سه `behind` — و طول محتوا با نسبت ۳٫۹۴. موضوع‌های
پوشش‌نداده هم درست بیرون آمدند («ماراتن»، «راهنمای»، «مقایسه»...).

سه مسیرِ صداقتِ گزارش هم روی داده‌ی واقعی دیده شدند: خزشی با سقف دو صفحه در
`ignored` نام برده شد و در داوری‌ها شرکت نکرد، شناسه‌ی خزشی که وجود نداشت در
`unavailable` آمد و مقایسه با بقیه تمام شد، و وقتی فقط یک رقیبِ قابل‌مقایسه
ماند فهرست موضوع‌ها خالی شد — چون قاعده «دست‌کم دو رقیب» است. تنانسی هم بررسی
شد: کاربر دوم روی مقایسه‌ی کاربر اول ۴۰۴ گرفت، فهرستش خالی بود، و وقتی خواست
خزش‌های کاربر اول را مقایسه کند کار با `UnknownRecord` شکست خورد.

**یک باگ واقعی از همین خروجی بیرون آمد:** در فهرست موضوع‌ها عبارت «مسابقه،» با
ویرگول دیده شد. نقطه‌گذاری فارسی داخل بلوک یونیکد عربی است و الگوی جداکننده‌ی
کلمه آن بلوک را دست‌نخورده نگه می‌داشت، پس ویرگول به کلمه‌ی قبلش می‌چسبید و
«مسابقه،» با «مسابقه» جور نمی‌شد. این روی تطبیق کلمات کلیدی سرویس Content هم
اثر داشت و از روز اول آنجا بود؛ حالا در `shared/text.py` اصلاح و تست شده.

### مقایسه داخل گردش‌کار

همان سه سایت، این بار از طریق یک گردش‌کار: `POST /api/v1/workflows` با
`competitors` و هیچ کار دستی دیگری. ده مرحله ساخته شد و روی RabbitMQ واقعی
اجرا شد — شش مرحله‌ی همیشگی، سه خزش رقیب پشت سر هم، و مقایسه. گزارش نهایی
`competitors_compared: 2` و `behind_on: 3` داشت، و سند رندرشده بخش «موضوع‌هایی
که رقبا پوشش می‌دهند و شما نه» را با ده عبارت آورد. داشبورد هم همان را نشان داد
و ده مرحله را با نام فارسی («خزش سایت رقیب»، «مقایسه با رقبا») فهرست کرد.

رقیب سوم عمداً یک پورت بسته بود. خزشش «انجام شد» با یک صفحه و صفر صفحه‌ی
ایندکس‌پذیر، و مقایسه آن را در `ignored` گذاشت — نه در داوری‌ها. یعنی مسیر
«نمونه‌ی خیلی کوچک» این بار با داده‌ی واقعیِ یک سایت در دسترس‌نبودن دیده شد، نه
با ورودی ساختگی.

**یک باگ از همین اجرا:** هر دو سایت آزمایشی روی `127.0.0.1` بودند و planner
رقبا را با «هاست» یکتا می‌کرد، پس دوتاشان یکی حساب شدند. حالا کلید یکتاسازی
**origin** است (هاست + پورت): دو پورت روی یک ماشین دو سایت‌اند، و
`www.rival.test/pricing` و `rival.test/` همچنان یکی.

### اجرای روند

سه اجرای پشت سر هم روی یک سایت محلی، از طریق گیت‌وی و روی RabbitMQ و Postgres
واقعی. بین اجرای اول و دوم خود سایت عوض شد: هر صفحه توضیح متا گرفت و طول
محتوا از ۸۱ به ۳۰۲ کلمه رسید.

اجرای اول `trend` نداشت — چون اولین بود، و «چیزی برای مقایسه نیست» با «چیزی
عوض نشده» یکی نیست. اجرای دوم امتیاز محتوا را ۶۸ ← ۷۶ با جهت `better` نشان
داد و خلاصه‌اش با «۱ سنجه بهتر شده و هیچ‌کدام بدتر نشده» شروع شد. `history` هم
هر سه اجرا را با تاریخ و امتیازشان برگرداند.

**دو ایراد از همین اجراها بیرون آمد.** اول: اجرای دوم «تغییر معناداری نیست»
گرفت، چون فقط سنجه‌های headline مقایسه می‌شدند و امتیاز کلی و تعداد ایراد
اتفاقاً تکان نخورده بودند — حالا امتیاز دسته‌ها هم مقایسه می‌شود. دوم: جدول
روند دسته‌ها را با کلید انگلیسی («content»، «ai_search») نشان می‌داد، چون
`crawl.completed` عمداً فقط کلید و امتیاز را حمل می‌کند و برچسب فارسی را نه؛
ترجمه حالا سمت ارکستریتور است.

### نمودار سابقه در داشبورد

سه اجرای کامل روی یک سایت، از طریق خود ماشین حالت (`engine.start` و
`engine.on_completion`) ساخته شدند تا گزارش‌ها — و بخش روند — را همان کدی
بسازد که در production می‌سازد؛ فقط باس جایش را به فراخوانی مستقیم داد. سه
اجرا امتیاز ۴۵، ۵۲ و ۶۱ داشتند.

بعد داشبورد با Chromium روی گیت‌وی واقعی درایو شد: پنل «سابقه‌ی این سایت»
نمودار را با نقاط `0,33 160,29 320,23` رسم کرد (بالا رفتن امتیاز روی محور ثابت
۰ تا ۱۰۰)، جدول هر سه اجرا را با امتیاز و تعداد ایراد آورد، لینک به اجرای
قبلی کار کرد، و اجرای اول درست هیچ بخش «نسبت به اجرای قبلی» نداشت ولی سابقه‌اش
را نشان داد.

**یک ایراد از همین اجرا:** نمودار اول با ارتفاع ۶۰ و `preserveAspectRatio=
"none"` کشیده می‌شد، و رشد ۱۶ امتیازی روی محور ۰ تا ۱۰۰ عملاً یک خط صاف
می‌شد — یعنی همان دروغی که با ثابت‌کردن محور جلویش را گرفته بودیم، این بار از
سمت نسبت ابعاد. حالا جعبه بلندتر است، نسبت ابعاد دست‌نخورده، و هر اجرا یک نقطه
دارد.

### بررسی compose بدون داکر

`docker-compose.yml` سی سرویس دارد و هرگز اجرا نشده. نوشتن این جمله در همین
سند صادقانه است ولی کاری نمی‌کند؛ `infra/tests/test_compose.py` هر دسته خطایی
را که **بدون دیمن قابل تصمیم‌گیری است** روی هر اجرای تست می‌گیرد: وجود
Dockerfile، وجود ماژولی که هر سرویس اجرا می‌کند، وجود `app` در فایلی که uvicorn
سرو می‌کند، کپی‌شدن پکیج و `shared/` داخل ایمیج، اشاره نکردن هیچ URL ای به
سرویسی که وجود ندارد یا به یک worker، خواندن پورتی که طرف مقابل واقعاً رویش
گوش می‌دهد، یکتا بودن پورت‌های میزبان، تعریف‌شدن هر volume، و ست‌بودن هر
متغیری که کد بدون آن بالا نمی‌آید.

**دو ایراد واقعی همان اولین بار بیرون آمد.**

یکم: **هیچ‌چیز مهاجرت‌ها را روی یک پشته‌ی در حال اجرا اعمال نمی‌کرد.** مهاجرت‌ها
به `/docker-entrypoint-initdb.d` مانت شده بودند، که ایمیج Postgres فقط روی boot
اولِ یک volume خالی اجرا می‌کند و هیچ‌جا ثبت نمی‌کند چه چیزی اجرا شده — یعنی
مهاجرت چهاردهم هرگز نمی‌رسید. حالا یک سرویس `migrate` هست که
`infra/db/migrate.sh` را اجرا می‌کند و ۲۳ سرویس دیگر با
`service_completed_successfully` منتظرش می‌مانند. آن اسکریپت روی Postgres واقعی
اجرا شد: روی یک دیتابیس خالی هر سیزده مهاجرت را اعمال کرد، بار دوم صفرتا، و
مهاجرتی که عمداً وسطش خطا می‌داد نه جدولش ماند نه ردیفش — و خروجی‌اش غیرصفر شد،
که همان چیزی است که `service_completed_successfully` به آن تکیه می‌کند.

نکته‌ی مهم این بود که آن دو مکانیزم **با هم جمع نمی‌شوند**: اگر مانت initdb سر
جایش می‌ماند، `migrate` یک `schema_migrations` خالی در برابر اسکیمای کامل
می‌دید و روی اولین `CREATE TABLE` می‌شکست. پس مانت برداشته شد، نه اینکه هر دو
بمانند.

دوم: **`PUBLIC_BASE_URL` روی `localhost:8080` بود و گیت‌وی روی `8000` منتشر
می‌شود.** این تنها تنظیمی است که مقدارش از شبکه بیرون می‌رود — لینکی که وبهوک
به یک آدم می‌دهد — و هیچ‌چیز داخل پشته دنبالش نمی‌رود، پس تا وقتی کسی روی لینک
کلیک نمی‌کرد معلوم نمی‌شد.

## قدم بعدی

۱. بالا آوردن docker-compose به‌صورت کامل (اینجا داکر دیمن نبود؛ همه‌ی اجزا
   تک‌تک روی Postgres، RabbitMQ و Redis واقعی اجرا شدند، ولی نه از طریق
   compose. آنچه بدون دیمن قابل بررسی بود حالا ۲۱ تست خودکار دارد)
۲. تأیید provider سرویس SERP و منابع تحقیق کلمات کلیدی روی شبکه‌ای که موتور
   جستجو و autocomplete را بلاک نمی‌کند
۳. اجرای Optimizer با `ANTHROPIC_API_KEY` واقعی
۴. سرویس‌های GSC، Backlinks و Analytics — هر سه به API خارجی (Search Console،
   ابزار بک‌لینک، GA4) نیاز دارند که از این محیط در دسترس نیست
۵. بقیه‌ی پوشه‌های `agents/` (هفت‌تا)، و k8s/terraform/monitoring
